# { config, lib, pkgs, ... }:
# with lib;

# let
#   cfg = config.programs.minecraft-client;
# in {
#   options.minecraft-client = {
#     enable = mkEnableOption "Minecraft client";
#     xwaylandDisplay = mkOption {
#       type = types.str;
#       default = ":12";
#     };
#   };

#   config = mkIf cfg.enable ({
#     home.packages = [
#       pkgs.prismlauncher
#       pkgs.xwayland-satellite
#     ];
#   } // mkIf config.niri.enable {
#     home = {
#       packages = [
#         pkgs.writeShellScriptBin "minecraft-prism" ''
#           set -eu

#           systemctl --user start xwayland-satellite-minecraft.service || true
#           export DISPLAY=${cfg.xwaylandDisplay}
#           export _JAVA_AWT_WM_NONREPARENTING=1
#           exec ${pkgs.prismlauncher}/bin/prismlauncher "$@"
#         ''
#       ];
#       sessionVariables = {
#         MINECRAFT_DISPLAY = xwaylandDisplay;
#       };
#     };

#     systemd.user.services.xwayland-satellite-minecraft = {
#       Unit = {
#         Description = "Xwayland satellite for Minecraft on Niri";
#         After = [ "graphical-session.target" ];
#         PartOf = [ "graphical-session.target" ];
#       };

#       Service = {
#         ExecStart = "${pkgs.xwayland-satellite}/bin/xwayland-satellite ${cfg.xwaylandDisplay}";
#         Restart = "on-failure";
#         RestartSec = 2;
#       };

#       Install.WantedBy = [ "graphical-session.target" ];
#     };

#     programs.niri.settings.spawn-at-startup = [
#       {
#         argv = [ "systemctl" "--user" "start" "xwayland-satellite-minecraft.service" ];
#       }
#     ];
#   });
# }
